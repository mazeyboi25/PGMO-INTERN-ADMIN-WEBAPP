# GitHub Upload Checklist

- Upload the **GitHub-Ready** folder only.
- Confirm both `config.local.js` files contain no real values before a web upload.
- When using Git, confirm `git status --ignored` lists both local config files as ignored.
- Search the staged files for passwords, hashes, API secrets, private tokens, and service-role keys.
- Do not upload the private credentials-included archive.
